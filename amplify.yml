version: 1
frontend:
    phases:
      preBuild:
        commands:
          - nvm install 15
          - yarn install
          - yarn blocks:build
      build:
        commands:
          - yarn web:build
    artifacts:
      baseDirectory: packages/gatsby-starter-rockinblocks/public
      files:
        - "**/*"
    appRoot: /
