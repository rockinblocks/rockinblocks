version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - nvm install 14
            - yarn install
            - yarn clean
        build:
          commands:
            - yarn web:build --verbose
      artifacts:
        baseDirectory: packages/gatsby-starter-rockinblocks/public
        files:
          - '**/*'