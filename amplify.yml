version: 1
applications:
  - gatsby:
      phases:
        preBuild:
          commands:
            - nvm install 14
            - yarn install
        build:
          commands:
            - yarn blocks:build
            - yarn web:build
      artifacts:
        baseDirectory: packages/gatsby-starter-rockinblocks/public
        files:
          - "**/*"
    appRoot: /
