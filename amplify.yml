version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - nvm install 14
            - yarn install
            - yarn clean
        build:
          commands:
            - yarn gatsby build --verbose
      artifacts:
        baseDirectory: public
        files:
          - '**/*'
    appRoot: packages/gatsby-starter-rockinblocks